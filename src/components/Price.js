import Form from './Form'

const Price = () => {
    return (
        <div>
            <Form buttonValue='set' buttonStyle='set' formStyle='form-price' placeholder='the price' />
        </div>
    )
}


export default Price
